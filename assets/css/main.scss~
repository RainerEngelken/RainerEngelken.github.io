---
---
@use "sass:color";
@use "sass:math";
@use "sass:map";
@use "sass:string";



@charset "utf-8";

// Dimensions
$max-content-width: {{ site.max_width }};

.publications .preview {
  max-width: 300px;
}



@import
  "variables",
  "themes",
  "layout",
  "base",
  "distill",
  "cv",
  "tabs",
  "typograms",
  "font-awesome/fontawesome",
  "font-awesome/brands",
  "font-awesome/solid",
  "font-awesome/regular",
  "tabler-icons/tabler-icons.scss",
  "tabler-icons/tabler-icons-filled.scss",
  "tabler-icons/tabler-icons-outline.scss"
  
  
.join-lab-button {
  // Use the color sampled from your profile picture's sweater
  background-color: #337a74;
  border-color: #337a74;
  color: #fff; // White text for contrast

  // Make it feel substantial and modern
  padding: 0.8rem 1.8rem;
  font-size: 1.2rem;
  font-weight: 500;
  border-radius: 0.5rem; // Slightly more rounded corners
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.15); // A subtle shadow for depth

  // A smooth transition for the hover effect
  transition: all 0.2s ease-in-out;
}

// Style for when the mouse is hovering over the button
.join-lab-button:hover {
  background-color: #2a655f; // A slightly darker shade for feedback
  border-color: #2a655f;
  color: #fff;
  transform: translateY(-2px); // Make it "lift" up slightly
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2); // A more pronounced shadow
}
