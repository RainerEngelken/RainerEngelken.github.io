# -----------------------------------------------------------------------------
# Site settings
# -----------------------------------------------------------------------------
title: "Rainer Engelken"
description: "Uncovering theoretical principles of learning and computation in neural circuits, from single neurons to brain-wide systems."
url: "https://rainerengelken.github.io" # base hostname for the deployed site
baseurl: "" # keep empty for user sites

lang: en
keywords: dynamical systems, neural networks, chaos, mean-field theory, spiking neural networks, Lyapunov exponents, gradient dynamics, learning
icon: "🧠"
last_updated: true
back_to_top: true

author:
  name: "Rainer Engelken"
  role: "Assistant Professor | Electrical & Computer Engineering | University of Illinois Urbana-Champaign"
  email: "engelken@illinois.edu"
  avatar: "/assets/img/RainerEngelken.jpg"
  scholar: "https://scholar.google.com/citations?user=HvZqeGQAAAAJ&hl=en&oi=ao"
  github: "https://github.com/RainerEngelken"
  twitter: "https://x.com/RainerEngelken"
  bluesky: "https://bsky.app/profile/rainerengelken.bsky.social"

# -----------------------------------------------------------------------------
# Theme and layout
# -----------------------------------------------------------------------------
navbar_fixed: true
footer_fixed: true
search_enabled: true
socials_in_search: true
posts_in_search: true
bib_search: true
max_width: 960px

serve_og_meta: true
serve_schema_org: true
og_image: /assets/img/RainerEngelken.jpg

# -----------------------------------------------------------------------------
# Analytics and verification (leave off for now)
# -----------------------------------------------------------------------------
enable_google_analytics: false
enable_cronitor_analytics: false
enable_pirsch_analytics: false
enable_openpanel_analytics: false
enable_google_verification: false
enable_bing_verification: false
google_analytics:
google_site_verification:
bing_site_verification:

# -----------------------------------------------------------------------------
# Blog (kept minimal; you can enable later)
# -----------------------------------------------------------------------------
blog_name: "Notes"
blog_description: "Short updates"
permalink: /blog/:year/:title/
lsi: false
pagination:
  enabled: true

related_blog_posts:
  enabled: true
  max_related: 5

giscus:
  repo:
  repo_id:
  category: Comments
  category_id:
  mapping: title
  strict: 1
  reactions_enabled: 1
  input_position: bottom
  theme: preferred_color_scheme
  emit_metadata: 0
  lang: en

disqus_shortname:

external_sources: []

# -----------------------------------------------------------------------------
# Collections (projects enabled by default in al-folio)
# -----------------------------------------------------------------------------
collections:
  books:
    output: true
  news:
    defaults:
      layout: post
    output: true
  projects:
    output: true

# -----------------------------------------------------------------------------
# Markdown and assets
# -----------------------------------------------------------------------------
markdown: kramdown
highlighter: rouge
kramdown:
  input: GFM
  syntax_highlighter_opts:
    css_class: "highlight"
    span:
      line_numbers: false
    block:
      line_numbers: false
      start_line: 1

include: ["_pages", "_scripts"]
exclude:
  - bin/
  - CONTRIBUTING.md
  - CUSTOMIZE.md
  - Dockerfile
  - docker-compose.yml
  - docker-compose-slim.yml
  - FAQ.md
  - Gemfile
  - Gemfile.lock
  - INSTALL.md
  - LICENSE
  - README.md
  - readme_preview/
  - vendor
keep_files:
  - CNAME
  - .nojekyll

sass:
  style: compressed

# -----------------------------------------------------------------------------
# Plugins (al-folio defaults)
# -----------------------------------------------------------------------------
plugins:
  - jekyll-archives-v2
  - jekyll-email-protect
  - jekyll-feed
  - jekyll-get-json
  - jekyll-imagemagick
  - jekyll-jupyter-notebook
  - jekyll-link-attributes
  - jekyll-minifier
  - jekyll-paginate-v2
  - jekyll-regex-replace
  - jekyll/scholar
  - jekyll-sitemap
  - jekyll-tabs
  - jekyll-terser
  - jekyll-toc
  - jekyll-twitter-plugin
  - jemoji

defaults:
  - scope:
      path: "assets"
    values:
      sitemap: false

jekyll-minifier:
  compress_javascript: false
  exclude: ["robots.txt", "assets/js/search/*.js"]

terser:
  compress:
    drop_console: true

# -----------------------------------------------------------------------------
# Jekyll Archives
# -----------------------------------------------------------------------------
jekyll-archives:
  posts:
    enabled: [year, tags, categories]
    permalinks:
      year: "/blog/:year/"
      tags: "/blog/:type/:name/"
      categories: "/blog/:type/:name/"
  books:
    enabled: [year, tags, categories]

display_tags: ["formatting", "images", "links", "math", "code", "blockquotes"]
display_categories: ["external-services"]

# -----------------------------------------------------------------------------
# Jekyll Scholar (use your name, not Einstein)
# -----------------------------------------------------------------------------
scholar:
  last_name: [Engelken]
  first_name: [Rainer, R.]
  style: apa
  locale: en
  source: /_bibliography/
  bibliography: papers.bib
  bibliography_template: bib
  bibtex_filters: [latex, smallcaps, superscript]
  replace_strings: true
  join_strings: true
  details_dir: bibliography
  details_link: Details
  query: "@*"
  group_by: year
  group_order: descending

enable_publication_badges:
  altmetric: true
  dimensions: true
  google_scholar: true
  inspirehep: false

filtered_bibtex_keywords:
  [
    abbr,
    abstract,
    additional_info,
    altmetric,
    annotation,
    arxiv,
    award,
    award_name,
    bibtex_show,
    blog,
    code,
    google_scholar_id,
    html,
    inspirehep_id,
    pdf,
    poster,
    preview,
    selected,
    slides,
    supp,
    video,
    website,
  ]

max_author_limit: 6
more_authors_animation_delay: 10
enable_publication_thumbnails: true

# -----------------------------------------------------------------------------
# Link attrs and images
# -----------------------------------------------------------------------------
external_links:
  enabled: true
  rel: "noopener noreferrer"
  target: "_blank"
  exclude:

imagemagick:
  enabled: true
  widths: [480, 800, 1400]
  input_directories: ["assets/img/"]
  input_formats: [".jpg", ".jpeg", ".png", ".tiff", ".gif"]
  output_formats:
    webp: "-quality 85"

lazy_loading_images: true

# -----------------------------------------------------------------------------
# Optional UI toggles
# -----------------------------------------------------------------------------
enable_masonry: true
enable_math: true
enable_tooltips: false
enable_darkmode: true
enable_navbar_social: true
enable_project_categories: true
enable_medium_zoom: true
enable_progressbar: true
enable_video_embedding: false

# -----------------------------------------------------------------------------
# Third-party library versions (leave as template defaults)
# -----------------------------------------------------------------------------
third_party_libraries:
  download: false
